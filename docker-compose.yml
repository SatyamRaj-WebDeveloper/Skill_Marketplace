version: '3.8'
services:
  mongo-db:
    image: mongo:latest
    ports:
      - 27017:27017
    volumes:
      - mongo-data:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=satyammongouser
      - MONGO_INITDB_ROOT_PASSWORD=mongo@satyam123
  
  backend:
    build : ./Backend
    ports:
     - 8000:8000
    environment:
     - MONGO_URI=mongodb://satyammongouser:mongo%40satyam123@mongo-db:27017/skillmarketplace?authSource=admin
     - JWT_SECRET=jhkdjsajlaskdpasldhuscascjaskcnaskkscks
     - NODE_ENV=development
     - PORT=8000
     - SEND_GRID_API_KEY=${SEND_GRID_API_KEY}
     - ADMIN_EMAIL=satyam.fullstackdeveloper@gamil.com
     - FROM=noreply@skillmarketplace.com
     - DASHBOARD_LINK=http://localhost:3000
    depends_on:
      - mongo-db
  

  frontend :
    build: ./frontend
    ports:
      - 3000:3000
    depends_on:
      - backend

volumes :
  mongo-data: